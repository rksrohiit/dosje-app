[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "dist"

# Redirect all traffic to index.html for React Router (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
