version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      # 1. Install dependencies for server only
      - npm --prefix server ci
run:
  command: node server/index.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: JWT_SECRET
      value: dosje_gov_aws_production_secret_key_2026
